[project]
name = "stock-analyzer"
version = "1.0.0"
description = "A股股票分析工具"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "akshare>=1.12.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
]

[project.scripts]
stock-analyzer = "main:main"